// **  Initial State
const initialState = {
    userData: {},
    isUnAuth: true,
    loading: false,
  };
  
  const socketReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default socketReducer;
  